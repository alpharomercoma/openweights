*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
*/build
*/*/build
/captures
.externalNativeBuild
.cxx
local.properties
*.keystore
*.jks
!debug.keystore
keystore.properties
/models/
*.gguf

# The picture MultimodalTest looks at. Generated by tools/red-octagon.py rather than
# committed, so that the fixture can be read and changed; the generator drops it in the
# working directory, where it was one `git add -A` away from being committed anyway.
/red-octagon.png

# Debris from the Playwright MCP browser tool, not project content.
.playwright-mcp/
build-mnn-host/
build-mnn-host-cl/

# CMake's own build directories under the Apple engine target, which the disposable
# build-ios-sim.sh script (and later, real ones) create beside the CMakeLists.txt they
# build from -- source only is committed, the same as every other CMake build directory in
# this project is kept out.
core/engine/src/main/cpp/apple/build-*/
