# The 2026-09-02 sweep: what six reviews found, what was measured, what changed

One session, the whole application, read without bias for regressions, vulnerabilities,
dead code and bad practice, and then a literature pass on what could make a 1 to 3 B
model on a phone faster or more accurate. Every finding below was verified against the
code before it was acted on; every number was measured on 2026-09-02 unless a date says
otherwise. Tests and lint were green before the sweep and the sweep found what they do
not see.

## Method

Six independent reviewers, each with one dimension and a brief to defend every claim
with a file and a line: the network-facing tool surface, local data and the sandbox, the
engine and native layer, the application loop, code hygiene and build configuration,
and a literature survey (2023 to 2026, arXiv and vendor engineering). Their findings
were re-read against the source, the highest-severity ones reproduced (the page-cleaner
timing on this machine, the routing measurement below on the app's own harness), and
fixed where a fix was small enough to land with a test. What was not fixed is listed
with the reason.

After the changes the host tiers are green: 1,237 unit tests (308 tools, 155 common,
125 data, 65 hub, 44 engine, 4 sandbox, 536 app), ktlint and detekt clean. The device
tier ran the same evening on a Snapdragon 8 Gen 3; what it settled, and what it found
that the host tiers cannot, is at the end.

## Fixed

Severity as verified, not as reported.

### Security

- **The canvas server served the shared folder to every app on the phone** (high).
  `CanvasServer` bound 127.0.0.1 and answered any `GET` with any workspace file, with no
  key and no host check, for the life of the process. Loopback is shared by every app,
  `INTERNET` is a normal permission, and the ephemeral port range scans in seconds. Now:
  a 128-bit key minted per start sits in every URL (`/key/...`) and a request without it
  gets the same 404 a missing file gets; the `Host` header must name this address and
  port; files are served only from under the canvas on screen (a site owns its folder, a
  document or deck owns one file, `Canvas.contains`); and dismissing the canvas closes
  the socket and forgets the key. The viewer shells reach their assets through the keyed
  base. `CanvasServerTest` covers each door.
- **A page of unclosed script tags held the engine for minutes** (high, measured). The
  furniture cleaner was a lazy regex, quadratic on unclosed tags: 256 KB took 26.7 s on
  this laptop, 512 KB 187 s, with no suspension point for Stop and the engine claimed
  throughout. Replaced by a single forward scan that remembers, per tag, that no close
  tag lies ahead. `PageFurnitureTest` pins 250 KB of it under three seconds and the
  cleaning semantics unchanged.
- **A fetched page could carry control tokens** (medium). The engine tokenizes the
  rendered prompt with special tokens parsed, so `<|im_end|>\n<|im_start|>system` inside
  a page became real tokens: a system turn the page wrote. Tool results from tools that
  return untrusted text are now combed for the ChatML, Llama 3, Phi, Gemma and Mistral
  spellings before they become messages (`withoutControlTokens`, `ControlTokensTest`).
- **`fetch_url save_to` overwrote any file** (medium). It called `put(..., replace =
  true)` with no ownership check and no approval, so a fetch the user approved for its
  address, or one that ran unasked as a turn's first call, could put a page over their
  notes. It now replaces only what this session created.
- **One approved overwrite made a user's file the session's** (medium). `write_file`
  recorded every successful put as a session artifact, so after one approved replace
  the file could be rewritten and deleted unattended, in a later conversation of the same
  process. Only files the session actually created are recorded now.
- **Remote GGUF headers had no ceiling on entry count or bytes fetched** (medium). A
  crafted header could keep the inspect-before-download parse streaming a multi-gigabyte
  file through 128 KB range requests on mobile data. Capped at 4,096 entries and 16 MB.
- **A picture tap launched any scheme** (low). The search provider's `url` field went to
  `ACTION_VIEW` unchecked; `tel:`, `sms:`, `market:` and app schemes now stay closed.
- **Tool HTTP calls were uncancellable and had no deadline** (medium). `execute()` on the
  IO dispatcher ignored cancellation, and the shared client's call timeout is zero because
  it also downloads models. Every tool call site now awaits an enqueued call that is
  cancelled with the coroutine, with a sixty-second ceiling for tool traffic.
- **A sandbox process that died mid-script was noticed only by the outer clock** (low).
  The binding-death callbacks now fail the pending result.

### Engine

- **A failed warm crashed the app** (high). The native warm throws for every failure
  but cancellation, `LlamaCppEngine.warm` let it through, and the caller is a bare
  `launch` after every load. A context window smaller than the head, a template that
  refuses a system-only render, or a decode refused under memory pressure was a crash
  on each load. The engine now reports null, as its contract says.
- **Sliding-window models were rolled back into a cache that had forgotten** (high,
  mechanism confirmed in llama.cpp, degradation not measured). `llama_memory_seq_rm`
  answers true for an iSWA cache whose local-attention cells past the window are gone,
  and `align_cache` took that as "the prefix is served". `llama.h` says to ask
  `llama_memory_seq_pos_min`, and llama-server does. The session now checks that the
  window reaches back from the cut, and treats sliding-window models like hybrids for
  the warm snapshot, so a new chat restores the head in milliseconds instead of
  re-reading it. Gemma 3 is the shipped family this reaches.
- **One ill-formed byte stalled the stream for the rest of the reply** (medium). The
  UTF-8 prefix scanner answered zero for "still arriving" and "never valid" alike, so a
  stray continuation byte at the front of the pending buffer stopped every later byte
  from being emitted while decoding continued. A malformed front byte is now replaced
  with U+FFFD and the stream moves on.
- **ExecuTorch: a Stop during prefill was erased, and every reply read as finished**
  (medium). The runtime clears its stop flag when the token loop starts and reports no
  reason; the engine now re-issues the stop from inside the callback (and when the
  collector has gone), and decides the reason itself: cancelled, end of turn, or cut for
  budget. A tool call parsed out of a stopped reply no longer runs. The comments that
  claimed `maxNewTokens` reaches the runtime and that prefill errors are discarded were
  wrong for 1.4.0 and say so now.

### Application

- **Regenerate ran a generation nothing could stop** (high). Its pre-generate job was
  never assigned to `generationJob`, so Stop cancelled a finished job, and a chat opened
  from the drawer meanwhile could receive the regenerated reply as its own. Tracked from
  its first line now, cancellation rethrown rather than read as a storage failure.
- **A warm could never supersede a running warm** (high). Cancelling the coroutine did
  not reach the native read, the replacement found the engine busy and gave up, and the
  old warm finished with the prompt it had. A tool toggle during the load warm, a reopen
  on a store miss, a new chat right after load: each warmed nothing. The new warm now
  interrupts the running one first, the way a turn does.
- **The tool-prompt fix of 2026-09-01 reached nobody who had opened the settings sheet**
  (high). The migration compared the stored prompt against two wordings that were not the
  one that shipped between the runtime merge and that fix. Added, version bumped to six,
  test seeds a version-five file with the real wording.
- **A fast watch's backstop cancelled itself** (medium). A tick ahead of the deadline
  returned the same null as a watch that is over, and the worker unscheduled it. The
  worker now asks whether the watch is still wanted before cancelling.
- **The previous chat's draft was seeded into the next** (medium). Cleared on switch.
- **Edit and actions state leaked across conversations** (medium). Ids restart at zero
  on reopen; both are keyed on the conversation now.
- **A chat left empty across midnight kept yesterday's date** (medium). The day is fixed
  at the first question as well as at the warm; the head warm never includes it, so
  nothing warmed is invalidated.

### Build, CI, hygiene

- `verifyOnDevice` ran zero tests for `:app` and `:core:engine` since the flavour split:
  it matched the exact task name, and the flavoured modules have
  `connectedStandardDebugAndroidTest`. Regex now, like `verify`.
- `verifyJniSymbols` read pre-flavour output paths, so it passed against a stale APK
  and AAB from before the split and was never wired to `bundleStandardRelease`, the task
  that produces what Play receives. Both fixed.
- The baseline profile could not be regenerated: the test module declared no runtime
  flavour and could not resolve `:app`. It declares both now.
- CI never ran `:core:common`'s 155 tests (`test` does not exist on a KMP module; it is
  `jvmTest`) and uploaded a debug APK from a path that no longer existed, which the
  action warns about and passes. Both fixed.
- detekt skipped the flavour source sets (the ExecuTorch JNI bridge lives there) and the
  instrumentation sources. Added.
- `:core:common` declared minSdk 29 against the project's 31. Aligned.
- 810 KB of unreferenced fonts deleted.

### Sampling, and the evals that measure it

- **The routing pass ran a sampler the routing evals never measured.** The app shipped
  `repeat_penalty = 1.1` over the last 64 tokens on every pass, including the greedy tool
  pass, while `eval/routing_matrix.py` and `eval/date_placement_eval.py` sent only
  temperature and so ran at llama-server's default of 1.0. The `SamplerParams` KDoc
  claiming llama.cpp's defaults was wrong: llama.cpp b10549 ships 1.0. The committed
  prompt dump was also stale (`search_files`, not `find_files`).
- Measured, both models, mode `plain`, the shipped catalogue, 33 cases, temperature 0,
  Mac llama-server:

  | model | rp 1.1 (app as shipped) | rp 1.0 (evals as run) |
  |---|---|---|
  | LFM2.5-1.2B QAD-Q4_0 | 27/33, date question calls `read_memory` | 28/33, date 1/1; every other case identical |
  | Qwen3-1.7B Q8_0, thinking on | 29/33 (capital of France searched; haiku -> write_file) | 29/33 (Pride and Prejudice searched; Riverlight invented) |

  The penalty flips one case on the shipped model, in the wrong direction, and is noise
  on Qwen3. `TurnRunner.deciding()` now drops it on the tool pass alongside forcing
  temperature 0; prose keeps the user's setting. Both evals send the app's tool-pass
  sampler in full, with env overrides, and the dump is regenerated.
- The rendered head on LFM2.5 with the 14-tool default catalogue is 2,209 tokens (336
  without tools). Per tool: run_script 216, watch 168, write_file 156, web_search 152,
  find_files 150, show_pictures 146, read_file 142, fetch_url 137, show_website 136,
  show_slides 121, show_document 121, save_memory 115, delete_file 108, read_memory 71.
  At the fitted b = 3.42e-6 s per token per token of context, 1,873 tool tokens cost
  6.4 ms on every generated token, about a tenth of decode time while tools are on,
  cache reuse notwithstanding. Trimming a third of the description text would buy about
  three percent of decode; it is not the lever, but it is a standing cost worth knowing.

## Found and not fixed, with the reason

Ranked. Each is a real finding with a file behind it; the reason it stayed is stated.

1. **The canvas WebView can reach any host, and the Markdown viewers render raw HTML**
   (`CanvasScreen.kt`, `doc.html`, `deck.html`). With the server now keyed and scoped,
   a model-built page can read only its own folder, but it can still POST that folder
   anywhere and a `.md` shown through `show_document` can run script. Blocking
   off-loopback traffic in `shouldInterceptRequest` and sanitising `marked` output are
   each an hour, but the first changes what a built site may load (CDN fonts, images)
   and is a product decision.
2. **The untrusted-text taint ages out** (`ToolNotes`, `TurnRunner`). The flag that gates
   egress after a fetched page is derived from the notes budget, which trims after a few
   later calls, while the engine record replays the page verbatim into every later
   prompt. A sticky taint on the record until the fold is the right shape; it touches
   the persisted conversation model.
3. **`Workspace.overwrite` truncates in place** (`Workspace.kt`). A provider error or
   process death mid-write leaves the original empty. Temp-then-rename is the fix and
   depends on each provider's rename support.
4. **ExecuTorch bookkeeping is not thread-confined** (`ExecuTorchEngine.kt`): a fold's
   summary chat can overlap a warm. Harmless on llama.cpp's single engine thread; on
   ExecuTorch it can duplicate text in the cache. Needs the same one-thread discipline.
5. **`hasEnabledTools()` ignores availability** (`TurnRunner.kt`): offline, the head
   still says search tools exist and forces thinking. **`ChatViewModel.stop()` cancels
   the engine globally**, truncating a running watch when it fires from reopen or delete
   with no chat turn running. **Returning to the chat reloads a model the user unloaded**
   (`OpenWeightsApp.kt`). **A turn whose final pass was an unrunnable call shows a reply
   that never reaches storage.** Each is a small change to a large file that wants its
   own test; listed here rather than rushed.
6. **The warm file carries no llama.cpp version** and the restore accepts any nonzero
   byte count; llama's structural checks are the protection today. **A JNI call after
   `CallVoidMethod` with a possibly pending exception** (`llama_jni.cpp`). **`read_file`
   offset is unbounded and skips by decoding.** **TokenVault has no fallback when the
   Keystore fails.** **Proxy credentials sit in plain SharedPreferences.** All low.
7. **Dead symbols**: `Tunable.MIN_P` (a hidden, unchangeable sampler the sheet never
   shows), `Watch.runsLeft`, `quantizationLabel`, `hasWebResearchEvidence`,
   `ChatRepository.compactionHistory`, `FitReport.headroomBytes`,
   `ExecuTorchFileName.isTokenizer`, `TaskPlan.MAX_BLOCK_TOKENS`,
   `ToolSwitches.isEnabled(String)`, `ChatUiState.supportsTools`, `Session::is_multimodal`,
   `raw_reply`, `NativeExecuTorchBridge.window`. Deletions, each a few lines, left for a
   pass of their own so this one stays reviewable.
8. **Host tests spend over seventy seconds sleeping** (`ChatFixture.settle`, 221 call
   sites, real `Thread.sleep`); `settleUntil` exists and has no callers.
   `ChatWarmTest` asserts against `LocalDate.now()`. `ToolCatalogueTest` and
   `PromptDumpTest` build their own tool lists rather than the registry's, so the
   catalogue tests cover ten of eighteen schemas and the eval dump can drift from the
   app (it had). `ChatViewModel.kt` is 3,988 lines behind a size suppression.
9. **`GoalSnapshotStore` commits on the UI thread**; **`CanvasServer` blocks an IO
   worker per connection** (bounded by the read timeout); **`ToolChoiceBenchmark`
   renders its own head** rather than `prefixMessages()`, so it measures a prompt the app
   does not send.

## Research: what the literature says, against what this app has measured

The survey covered draft-free speculation, context management, tool-calling accuracy at
1 to 3 B, reasoning budgets, quantisation and kernels on Arm, sampling, prompt design and
agent security, and one paper the user supplied (arXiv 2608.22472, "Small Reasoning
Models are Instruction Followers in Function Calling"). Where the app's own measurements
disagree with a paper, the app's win; they were taken on the hardware that ships.

### Worth building, in order

1. **Draft-free n-gram speculation, gated to copy-heavy spans.** The pinned llama.cpp
   already ships five self-speculative modes (`ngram-simple`, `ngram-map-k`,
   `ngram-map-k4v`, `ngram-mod`, `ngram-cache`; PR #19164) that draft only on a context
   match and cost nothing otherwise. The app's own batch measurement sets the break-even:
   a batch of 4 costs 1.68 single steps, a batch of 8 costs 3.09, so a 4-token draft pays
   once more than 0.68 of it is accepted and yields 3x at full acceptance; a wholly
   rejected draft costs 1.68x on that step only. Where it pays is where the model copies:
   canvas rebuilds, quoting fetched text, code edits, memory saves. Draft-model
   speculation stays rejected (measured 1.5 to 3x slower); this is a different cost
   profile. Hybrid targets (LFM2.5) need llama.cpp's checkpoint stash
   (`common_prompt_checkpoint`), which llama-server uses; transformers roll back directly.
   Experiment: measure the verify cost at batch 2, 4 and 8 on the phone, then a verify
   loop in `Session` with `n_match` 16 to 24 and `draft-max` 4, scored on the replay
   corpus and the canvas evals. Go if the median agentic turn gains 15 percent of wall
   clock and no chat turn loses more than two.
2. **A reasoning cap on the tool pass, not no-think.** The app forces thinking on when
   tools are offered because suppressing it cost LFM2.5-2.6B 15.5 points of abstention
   (commit 7c2fb65), and the user-supplied paper agrees: Qwen3-1.7B on BFCL v3 Live goes
   from 65.0 to 72.8 with thinking. But "Brief Is Better" (arXiv 2604.02155, Qwen2.5-1.5B
   on BFCL) finds the optimum at 8 to 32 reasoning tokens and 256 or more worse than
   none, with hallucinated function names rising from 3 to 20 percent. On the phone a
   thought is 20 to 50 seconds. Budget forcing (s1, arXiv 2501.19393): cap the tool
   pass's thinking at 32 to 128 tokens and close the block. The routing matrix with
   `MAX_TOKENS` and a forced `</think>` is the measurement; the thinking models in the
   catalogue are the subjects.
3. **Observation masking before the model-written fold.** The fold summarises with the
   model (24 to 34 s) and re-reads (10 to 14 s). "The Complexity Trap" (arXiv 2508.21433)
   finds replacing old tool observations with a placeholder matches or beats LLM
   summarisation on SWE-bench at half the cost, and that summaries lengthen trajectories.
   Measured at 32 B and up, so an extrapolation, but the mechanism is model-agnostic and
   it saves a generation pass on hybrids and ExecuTorch. It must happen only at the fold
   boundary, where the prefix is being rewritten anyway. Experiment: the long-conversation
   eval with tool rounds masked first, folded only when masking frees less than the
   3,000-token threshold.
4. **ExecuTorch rewind, by overwrite.** The runner's `pos_` is private and the JNI
   exposes only `prefillPrompt` and `resetContext`, so "no rollback API" is exact. But
   the runtime supports it: v1.4.0's `KVCache.update` writes at `input_pos` with
   `index_copy_`, the mask is a `tril` narrowed to the current rows, and the custom SDPA
   bounds keys to `start_pos + seq_len`, so re-prefilling from an earlier position
   overwrites the tail and the stale cells are never attended. A custom JNI over
   `TextPrefiller` with an explicit position gives regenerate, branch and fold the same
   partial rollback llama.cpp transformers have. `RingKVCache` exports (local-global
   attention) track `cache_positions` and need their own check.
5. **KV cache at q8_0 with flash attention on llama.cpp.** Decode is affine in context
   (a = 0.0600 s, b = 3.42e-6 s per token of context on the MT6991); halving the bytes
   attention reads at the 4,096 ceiling saves on the order of a tenth of decode there.
   K must never be q4_0 (llama.cpp discussion #23470: collapse); Gemma is
   KV-quantisation-sensitive (KL 0.1 to 0.38 at q8_0) and needs its own check. No Arm
   measurement exists; the sustained-use test is the place to take one.
6. **Compact tool schemas.** The head is 2,209 tokens. "Efficient On-Device Agents via
   Adaptive Context Management" (arXiv 2511.03728) cut a 3 B system prompt from 3,200 to
   400 tokens with no loss; exotic notations (TOON) cost 9 to 14 points and are out. The
   app's own finding that wording barely moves routing means the gain is decode time and
   prefill on a cold store, about three percent of decode per third of description text.

### Corroborated by the literature, already done here

Q4_0 and Q8_0 over Q4_K_M for the KleidiAI kernels (Arm's SME2 numbers on Llama-3.2-3B
agree with the app's 1.26x); lazy grammars that bind only after the call starts ("The
Constraint Tax", arXiv 2605.26128: hard schemas at 1.5 B double wrong-but-valid calls);
near-greedy tool passes (BFCL scores at temperature 0.001); the 4,096 fold ceiling
("Context Rot": accuracy falls with length even on Qwen3-8B); tool-count reduction below
18 not being the lever (shortlisting pays at 50 or more tools, and the app measured no
loss from two to six); keeping verbatim tool excerpts over summaries (Meta-Harness).

### From the user-supplied paper, and what transfers

IFFC decouples routing into a small reasoning model with a prompt-based call format and a
context free of schemas. For Qwen3-1.7B thinking it scores 72.8 on BFCL v3 Live against
74.6 native and 73.6 prompt-based: no routing gain at this size, and a second sequential
pass per turn. What transfers: thinking on the routing pass (Table 5, above), and the
quantisation result that thinking models hold at Q4KM (1.7B: 82.8 to 82.9) where
non-thinking 0.6B collapses (62.3 to 16.8), which is the same shape as the app's own
QAD-Q4_0 versus Q4_0 routing split and argues for measuring every routing verdict per
quant, as the docs already do.

### Not worth pursuing on this hardware

Draft-model speculation (measured), EAGLE-3 and Medusa heads, lookahead and tree
verification (compute-bound verifier), LayerSkip and Kangaroo (need trained checkpoints),
min-p and top-n-sigma (gains at high temperature only), NPU prefill offload (projected 1.07
to 1.47x), a grammar-gated routing pre-check (the app measured 63 percent, worse than
prompting), CaMeL-style dual-model interpreters, and any RL or SFT routing fix unless it
ships as weights.

## What the phone settled

Run the same evening on a Qualcomm Device Cloud SM8650 (Snapdragon 8 Gen 3, Android 14)
over the SSH tunnel to its adb server, on a fresh device with nothing installed. Fixtures
pushed: LFM2.5-1.2B QAD-Q4_0 as `model.gguf` and in the app's own folder, Qwen3-1.7B
Q8_0 as `bench/qwen.gguf`, Gemma 3 1B Q4_K_M as `bench/gemma.gguf`, and for the
accelerated flavour the Qwen3-1.7B INT8-INT4 XNNPACK `.pte` with its tokenizer. The suites
ran through `am instrument` with the three long benchmarks excluded; `verifyOnDevice`
itself now resolves to the flavoured app and engine tasks (`--dry-run` lists both), which
it matched none of before the sweep.

**App tier, 47 tests: 29 passed, 8 skipped for fixtures this device does not have (a
shared folder, a vision model, the eval directory), 10 failed on the first run.** Nine of
the ten were one cause and it was the environment: `adb shell` is root on this
reservation, the models folder made from it was root-owned, the app could not list it and
opened on "Pick a model to begin", and every composer test failed as if nothing were
installed. Recorded in CONTEXT.md with the `chown` that fixes it. With the folder owned
by the app, four of those nine pass; the five that remain (a typed `/plan` still in the
composer after Send, the palette not opening on `/`, pasted text and pasted pictures not
arriving) were then run on the pre-sweep build, commit 9c04026, on the same phone: the
same five fail there, so they are not regressions. They are a difference between this
device and the Poco, where the paste suite passed four of four on 2026-08-29, and the
suspects are clipboard access and the keyboard on this reservation; chased separately.
The tenth, `LongConversationOnDeviceTest` with tools on, re-read
2,544 prompt tokens on a follow-up; the test's own comment says it is left failing on
purpose for the tool-history shape that costs a hybrid its cache, and its control with
`-e tools 0` passed here in 64 seconds. Not a regression.

**Engine tier, 30 tests: 13 passed, 16 skipped for fixtures (six multimodal, the
sustained-use hybrid, nine evals), 1 failed, and the failure was the expectation in a test
written for this sweep, not the engine.** The new `SlidingWindowOnDeviceTest` drives
Gemma 3 1B through a rollback shorter than its 512-position window and one longer. Both
were reused: 1,158 tokens on the first turn, 11 on the short rollback, 1,151 on the long
one against about 2,340 for a full re-read, with the same legible reply each time. The
reason is a setting, not a bug: llama.cpp's default context keeps the window layers'
cache full (`swa_full`), this engine takes the default, and llama-server skips its own
window check under it. So the refusal path added to `align_cache` is correct and dormant.
It matters the day the engine turns the windowed cache on to save memory, because Gemma 3
today pays a full-context cache on every layer, and that day a regenerate or an edit more
than 512 tokens back costs a full re-prefill. That is a trade to take with numbers, and it
is now on the list below rather than assumed either way.

**ExecuTorch, 7 tests, 7 passed after two corrections to the tests.** The cancel fix holds
on the real runtime: a Stop issued after five streamed tokens halted it after one more and
was reported as cancelled. A budget of eight tokens is reported as the budget, not as a
finished turn; the first version of that test asked with reasoning off and the model
answered the counting prompt in two tokens and ended by itself, which is a different
case, so it now asks with reasoning on. And the second-turn test had been asking for a
48-token budget while its own comment prescribes the default: a cut reply drops the cache
by design (engine behaviour from 2026-08-31, not this sweep), so it re-read all 971 tokens.
At the default budget the second turn cached 1,676 tokens, read 20, and prefilled in 328
ms against 6,295 cold: a ratio of 0.05.

**The routing benchmark on the phone, at the sampler the runner now uses** (temperature 0,
no repeat penalty, 160-token budget, reasoning on; six cases): Qwen3-1.7B Q8_0 right on
3, 3, 4 and 4 of 6 across the bare, tagged, reversed and warm arms, every miss an
under-call; Gemma 3 1B on 3, 5, 3 and 3. Twelve and a half minutes for the pair. The
benchmark had been forcing temperature zero while keeping the shipped 1.1 penalty, a
sampler the runner no longer uses; it now mirrors `deciding()` and takes `-e penalty 1.1`
to put the old one back for a comparison on one build and one phone. That comparison
ran: with the penalty back, Gemma 3 is identical on every arm and Qwen3 loses one case,
the reversed arm's `fetch_url` call becoming an under-call, 4 of 6 to 3 of 6. Six cases
are too few to call that a size, but the sign agrees with the thirty-three offline: the
penalty never helped a routing decision here, and once it cost one.

## What is still only a phone's to settle

In order: the windowed cache for Gemma 3 (`swa_full` off), measured as KV memory saved
against the cost of a refused rollback on regenerate and edit; the n-gram verify-cost
curve at batch 2, 4 and 8; and the KV q8_0 decode sweep.
