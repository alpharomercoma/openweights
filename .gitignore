*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
*/build
*/*/build
/captures
.externalNativeBuild
.cxx
local.properties
*.keystore
*.jks
!debug.keystore
keystore.properties
/models/
*.gguf

# The picture MultimodalTest looks at. Generated by tools/red-octagon.py rather than
# committed, so that the fixture can be read and changed; the generator drops it in the
# working directory, where it was one `git add -A` away from being committed anyway.
/red-octagon.png

# Debris from the Playwright MCP browser tool, not project content.
.playwright-mcp/
build-mnn-host/
build-mnn-host-cl/
build-ios-sim/

# The Kotlin compiler's session directory, written by Gradle builds on the host.
.kotlin/
