# Robolectric ships Android runtimes up to a fixed level and refuses anything newer than
# it has. The app compiles against 37 and targets 36, so tests run on the newest runtime
# Robolectric actually has rather than following compileSdk.
sdk=36
